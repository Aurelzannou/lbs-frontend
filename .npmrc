# @nebular@15 impose des peer deps Angular 15, incompatibles avec Angular 19 utilisé ici.
# legacy-peer-deps=true reproduit --legacy-peer-deps par défaut pour npm ci / npm install,
# nécessaire en local, en CI et dans le build Docker.
legacy-peer-deps=true
